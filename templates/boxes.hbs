 <h3>
  Current Boxes:
</h3>

<ul>
  {{#each}}
  <li>{{#link-to 'box' this.id}}{{name}}{{/link-to}} | <a href="#" {{action 'delete' this}}>Delete</a></li>
    <ul>
      <li>{{description}}</li>
    </ul>
  {{/each}}
</ul>
